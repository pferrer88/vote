<h2>Se te olvido tu contraseña?</h2>

<%= simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post, :class => "form-stacked" }) do |f| %>
  <%= devise_error_messages! %>

	<%= f.input :email, :input_html => { :class => 'span3 required' } %>
  
	<%= f.button :submit, :class => 'btn', :value => "Enviame las instrucciones" %>
<% end %>

<%= render :partial => "devise/shared/links" %>